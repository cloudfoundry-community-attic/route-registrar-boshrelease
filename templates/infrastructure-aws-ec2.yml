meta:
  environment: (( merge ))
  dns_root: (( merge ))
  security_groups: (( merge ))
  persistent_disk: 4096

  stemcell:
    name: bosh-aws-xen-ubuntu
    version: latest

jobs:
  - name: route_registrar_leader_z1
    instances: 1
    networks:
      - name: route_registrar1
    persistent_disk: (( meta.persistent_disk ))
  - name: route_registrar_z1
    instances: 2
    networks:
      - name: route_registrar1
    persistent_disk: (( meta.persistent_disk ))
    properties:
      route_registrar:
        leader_address: (( "0.route-registrar-leader-z1.route-registrar1." meta.environment "." meta.dns_root ))

compilation:
  cloud_properties:
    instance_type: m1.small

resource_pools:
  - name: small_z1
    cloud_properties:
      instance_type: m1.small

networks:
  - name: floating
    type: vip
    cloud_properties: {}
  - name: route_registrar1
    type: dynamic
    cloud_properties:
      security_groups: (( meta.security_groups ))
